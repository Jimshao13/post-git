<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.post.db.mapper.PredictMapper">

    <select id="arrivalsToday" resultType="Integer">
        select count(*)
        from p_predict_data,provinces
        where p_predict_data.originId=provinces.provinceid AND datediff(curDate(),p_predict_data.startTime)=provinces.time;
    </select>


</mapper>